#!/usr/bin/env bash
# Start a local CAT-SOOP server pointing at a course directory.
#
# Usage: ./run-local.sh <path-to-course-dir>
#
# TODO: fill in the actual steps. Sketch:
#   1. Activate the venv created by install.sh.
#   2. Point CAT-SOOP at the course directory (env var? config file?).
#   3. Start the server (which command? what port?).
#   4. Print the URL the user should open.

set -euo pipefail

echo "TODO: implement local-server startup"
exit 1
