# Troubleshooting CAT-SOOP setup

Symptom-first. Add entries as you hit them.

## Install

<!-- TODO: e.g., "pip install fails with X" → "downgrade to Python 3.Z". -->

## Server starts but ...

<!-- TODO: e.g., "all pages 404" → "course directory env var not set". -->

## Auth / users

<!-- TODO: e.g., "can't log in" → "create __USERS__/<kerb>.py with role = ...". -->

## Content not rendering

<!-- TODO: e.g., "edits don't show up" → "clear cache at <path>". -->
