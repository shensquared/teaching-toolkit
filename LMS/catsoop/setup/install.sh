#!/usr/bin/env bash
# Install CAT-SOOP and its dependencies into a local venv.
#
# Usage: ./install.sh
#
# TODO: fill in the actual steps. Sketch:
#   1. Check Python version (which versions work?).
#   2. Create / activate a venv (where? e.g., ./.venv).
#   3. pip install catsoop (or clone + install from source?).
#   4. Verify the install (e.g., `catsoop --version`).

set -euo pipefail

echo "TODO: implement install steps"
exit 1
